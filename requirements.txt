# Core inference & detection
ultralytics>=8.3.0          # YOLO11 + tracking support
torch>=2.1.0                # PyTorch backend
torchvision>=0.16.0         # Required by ultralytics

# Image / video processing
opencv-python>=4.9.0        # Video capture, drawing, codec support
Pillow>=10.0.0              # Image loading (PIL) for Streamlit uploader
numpy>=1.26.0               # Array ops

# Web UI
streamlit>=1.35.0           # App framework